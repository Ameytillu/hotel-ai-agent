# hotel-ai-agent
A browser-based AI assistant for hotels — built with Python, Flask, and machine learning. It analyzes guest reviews, answers hotel-related questions using a custom FAQ knowledge base (RAG), and uses Model Context Protocol (MCP) to handle guest intent and smart actions.
